import MainLayout from "@/Layouts/MainLayout";

export default function Dashboard() {
    return (
        <MainLayout>
            <div className="min-h-screen text-white flex items-center justify-center">
                You have been logged in... for now.
            </div>
        </MainLayout>
    );
}
